package cmd

import (
	"workingcli/src/utils"
)

// ConfirmWithDefault는 utils 패키지의 함수를 재사용합니다.
var ConfirmWithDefault = utils.ConfirmWithDefault

// Confirm은 utils 패키지의 함수를 재사용합니다.
var Confirm = utils.Confirm

// UnescapeGitPath는 utils 패키지의 함수를 재사용합니다.
var UnescapeGitPath = utils.UnescapeGitPath

// ProcessGitOutput은 utils 패키지의 함수를 재사용합니다.
var ProcessGitOutput = utils.ProcessGitOutput

// ProcessGitPaths는 utils 패키지의 함수를 재사용합니다.
var ProcessGitPaths = utils.ProcessGitPaths 